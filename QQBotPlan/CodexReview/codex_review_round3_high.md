已切换为该审核模式。

我会重点检查这些方面：
1. 前后端接口契约一致性（URL、方法、参数、返回结构、错误码）
2. 类型与字段映射一致性（必填/可选、命名、枚举、默认值）
3. 鉴权与跨域链路（Token、Cookie、CORS、权限边界）
4. 异常处理与重试策略（超时、幂等、降级、错误透传）
5. 版本兼容与发布风险（向后兼容、灰度、回滚）

把你要审核的代码范围或任务文档路径发我，我直接开始并输出分级审核报告。