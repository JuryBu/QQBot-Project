我发现当前 Git 工作区包含大量与本任务无关的外层目录变更（超出 `AstrBotLauncher-0.1.5.6` 代码范围），这会影响基于 `git diff` 的准确审查。请确认是：

1. 继续仅按 `AstrBot/data/plugins/astrbot_plugin_flashlite/main.py` 的 R1 相关代码做“定点审核”
2. 你提供本次 R1 的提交范围（commit/文件列表），我按精确变更审核