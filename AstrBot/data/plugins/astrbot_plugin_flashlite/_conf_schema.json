{
    "sync_trigger_interval": {
        "description": "同步触发间隔（每 N 条群消息触发一次 Flash Lite）",
        "type": "int",
        "hint": "建议 3-10，越小调用越频繁",
        "default": 5,
        "minimum": 2
    },
    "sync_time_interval": {
        "description": "时间兜底触发间隔（秒）",
        "type": "int",
        "hint": "超过该时间且达到最低消息数则触发同步",
        "default": 60,
        "minimum": 10
    },
    "sync_time_min_msgs": {
        "description": "时间兜底触发最低消息数",
        "type": "int",
        "hint": "时间兜底需要至少积累这么多条消息才触发（防止低活群频繁触发浪费 API）",
        "default": 3,
        "minimum": 1
    },
    "sampling_mode": {
        "description": "采样模式",
        "type": "string",
        "hint": "dynamic=智能动态调整间隔, fixed=固定间隔",
        "default": "dynamic",
        "options": ["dynamic", "fixed"]
    },
    "dynamic_sampling": {
        "description": "动态采样配置（JSON格式，sampling_mode=dynamic 时生效）",
        "type": "string",
        "hint": "包含 window_minutes(滑动窗口分钟)、thresholds(活跃度阈值列表)、intervals(对应间隔列表)。示例: {\"window_minutes\":10,\"thresholds\":[5,15,30],\"intervals\":[3,5,10,15]}",
        "default": "{\"window_minutes\":10,\"thresholds\":[5,15,30],\"intervals\":[3,5,10,15]}"
    },
    "group_overrides": {
        "description": "每群独立采样配置（JSON格式）",
        "type": "string",
        "hint": "按群号覆盖全局采样间隔。格式: {\"群号\":{\"sync_interval\":10,\"enabled\":true}}",
        "default": "{}"
    },
    "wake_keywords": {
        "description": "唤醒关键词列表",
        "type": "string",
        "hint": "逗号分隔的关键词列表，出现任一关键词立即触发",
        "default": "老板娘,boss"
    },
    "gemini_api_key": {
        "description": "Gemini API Key（留空则自动从 AstrBot 配置读取）",
        "type": "string",
        "hint": "可为空，引擎会自动搜索 cmd_config.json 中的 key",
        "default": ""
    },
    "thinking_level": {
        "description": "Flash Lite 思考级别",
        "type": "string",
        "hint": "LOW/MEDIUM/HIGH，建议 MEDIUM",
        "default": "MEDIUM"
    },
    "max_context_messages": {
        "description": "判断时最多读取多少条最近消息",
        "type": "int",
        "hint": "越多上下文越准确，但 token 开销也越大",
        "default": 15,
        "minimum": 5
    },
    "review_interval_hours": {
        "description": "Sandbox 定期 Review 间隔（小时）",
        "type": "int",
        "hint": "建议 12-48，设为 0 关闭定期 Review",
        "default": 24,
        "minimum": 0
    },
    "model": {
        "description": "FlashLite 主判断模型名",
        "type": "string",
        "hint": "面板可切换；缺省回退模块常量。读取于 main.py:127 self._model",
        "default": "gemini-3.1-flash-lite-preview"
    },
    "tool_model": {
        "description": "工具模型配置（用于 search(scope=web) 等工具场景）",
        "type": "object",
        "hint": "model=模型名, thinking_budget=思考预算, api_keys=Key 池(Round-robin 轮转)。读取于 main.py:132-137",
        "items": {
            "model": {
                "description": "工具模型名",
                "type": "string",
                "hint": "留空则回退 TOOL_MODEL_DEFAULT",
                "default": ""
            },
            "thinking_budget": {
                "description": "工具模型思考预算（token）",
                "type": "int",
                "hint": "默认 2048",
                "default": 2048
            },
            "api_keys": {
                "description": "工具模型 API Key 池",
                "type": "list",
                "items": {"type": "string"},
                "hint": "Round-robin 轮转 + 429 自动切换，留空则复用主 key"
            }
        }
    },
    "image_model": {
        "description": "图像生成模型配置",
        "type": "object",
        "hint": "model=图像模型名。读取于 main.py:4661-4662",
        "items": {
            "model": {
                "description": "图像生成模型名",
                "type": "string",
                "hint": "默认 gemini-2.5-flash-image",
                "default": "gemini-2.5-flash-image"
            }
        }
    },
    "checkpoint_limit": {
        "description": "CHECKPOINT token 上限",
        "type": "int",
        "hint": "超过该 token 数触发压缩。读取于 main.py:197/2979",
        "default": 50000,
        "minimum": 1000
    },
    "checkpoint_keep_recent": {
        "description": "CHECKPOINT 保留最近条数",
        "type": "int",
        "hint": "压缩时保留最近 N 条原文。读取于 main.py:198/2980",
        "default": 10,
        "minimum": 1
    },
    "checkpoint_compress_front_ratio": {
        "description": "CHECKPOINT 前段压缩比例",
        "type": "float",
        "hint": "压缩时前段所占比例（0-1）。读取于 main.py:2981",
        "default": 0.7
    },
    "checkpoint_cooldown_seconds": {
        "description": "CHECKPOINT 压缩冷却时间（秒）",
        "type": "int",
        "hint": "两次压缩最小间隔。读取于 main.py:2982",
        "default": 300,
        "minimum": 0
    },
    "checkpoint_target_min": {
        "description": "CHECKPOINT 压缩目标下限比例",
        "type": "float",
        "hint": "压缩后目标 token 占上限的最小比例（0-1）。读取于 main.py:2983",
        "default": 0.20
    },
    "checkpoint_target_max": {
        "description": "CHECKPOINT 压缩目标上限比例",
        "type": "float",
        "hint": "压缩后目标 token 占上限的最大比例（0-1）。读取于 main.py:2984",
        "default": 0.40
    },
    "cost_usd_to_cny": {
        "description": "美元转人民币汇率",
        "type": "float",
        "hint": "成本统计用。读取于 main.py CostTracker(usd_to_cny=...)",
        "default": 7.2
    },
    "cost_custom_pricing": {
        "description": "自定义模型定价表（JSON 格式）",
        "type": "string",
        "hint": "动态模型名子键，必须 JSON 字符串打包（不可 object 展开，否则被框架递归删）。格式: {\"模型名\":{\"input\":0.1,\"output\":0.4}}。运行时 json.loads 解析",
        "default": "{}"
    }
}
